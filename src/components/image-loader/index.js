import { PolymerElement, html } from '@polymer/polymer';


class ImageLoader extends PolymerElement {
    static get template()  {
         return html`
         
         `
}
    ready(){
        super.ready()
 
        }
    static get properties() { return {
    }}
}
window.customElements.define('image-loader', ImageLoader);